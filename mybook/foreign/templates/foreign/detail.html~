{% extends "foreign/base.html" %}

{% block title %}Question List{% endblock title %}

{% block content %}
<h2 class="mt-4 border-bottom">Question {{ question.id }}: {{ question.question_text }}</h4><br>
<h3><font color="red">{{ error_message }}</font></h3><br>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>ID</th>
      <th>Choice</th>
      <th>Vote</th>
    </tr>
  </thead>
  <form action="{% url 'foreign:vote' question_id=question.id %}" method="post">
    {% csrf_token %}
    <tbody>
      {# {% for choice in choices %} #}
      {% for choice in question.choice_set.all %}
      <tr>
	<td>{{ forloop.counter }}</td>
	<td>
	  <input type="radio" name="choice" id="choice{{ forloop.counter }}" value="{{ choice.id }}">
	  {{ choice.choice_text }}
	</td>
	<td>{{ choice.vote }}</td>
      </tr>
      {% endfor %}
  </tbody>
</table>
<input type="submit" value="Vote">

{% endblock content %}
